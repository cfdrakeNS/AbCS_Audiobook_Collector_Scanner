# AbCS Refactoring – Fallback & Correction Logic (Feb 28, 2026)

## Goal
Consolidate and refactor repeated fallback and correction logic in import scanner and validator modules to ensure a single, clear code path for each scenario.

---

## Current State
- Fallback logic for author/title is repeated in multiple places in `import_scanner.py`.
- Correction logic (punctuation, case, special character removal) is applied in several blocks, sometimes redundantly.
- Error/correction flagging uses string prefixes (F:, C:, W:, E:) and is checked in both scanner and validator.
- Error categorization in `validator.py` uses string matching and severity rules, but warning/parse/read/duplicate logic is spread across methods.

---

## Recent Changes (Feb 28, 2026)
- Updated ImportWindow and PreferencesWindow to use checkboxes for author/title fallback controls instead of combo boxes, improving accessibility and clarity.
- Fixed initialization and signal connection order for fallback checkboxes to resolve runtime errors.
- Removed duplicate and misplaced layout/header code in ImportWindow to fix 'layout is not defined' errors in VS Code.
- Moved format_checkbox_style definition above its usage in PreferencesWindow to resolve UnboundLocalError.
- Verified all changes with pytest; no Python errors remain in patched files, but one test failure in import window rules remains for further review.
- Updated abcs_code_review_feb28.md to reflect these changes and document the refactor.

---

## Additional Changes (Feb 28, 2026)
- Refactored ImportScanner.configure to only apply fallback logic if enabled in preferences (no default override).
- Fixed ImportWindow height restoration: moved resize() call to immediately after UI setup to ensure correct window size.
- Increased reader keywords field width in PreferencesWindow for usability.
- Verified all changes by running the app; no runtime errors and UI changes are visible.
- All fallback/correction logic now respects user preferences and does not apply unless enabled.

---

## Refactoring Plan
1. **Unify Fallback Logic**
   - Fallback preferences for author and title are now controlled by checkboxes in the ImportWindow header:
     - "Author fallback to folder?" (checked = fallback enabled)
     - "Title fallback to file?" (checked = fallback enabled)
   - Old logic for title fallback to folder is removed; only file fallback is available for title.
   - Fallback logic is unified in `ImportScanner.apply_preferences`, with settings passed from ImportWindow.
   - Checkboxes update preferences and reconfigure ImportScanner immediately when toggled.
   - Only one fallback is applied per field, following the configured preference order.

2. **Centralize Correction Logic**
   - Create a unified correction method for punctuation, case, and special character handling.
   - Apply corrections once per field, and flag only if a change is made.
   - Remove redundant correction blocks.

3. **Standardize Error/Correction Flagging**
   - Use a single function to append error/correction flags to book records.
   - Ensure all flags use consistent prefixes and are categorized in `validator.py`.
   - Refactor error categorization to use normalized messages and severity rules only.

4. **Testing & Validation**
   - Run import scenarios with various error/fallback/correction cases.
   - Verify that only one fallback/correction is applied per field and all flags are correctly categorized.
   - Confirm UI and status bar reflect correct error/correction states.

---

## Next Steps
- Implement unified fallback and correction methods in `import_scanner.py`.
- Refactor error/correction flagging and categorization in `validator.py`.
- Remove redundant code blocks and test thoroughly.
- Document changes and update code review notes.

---

*Generated by GitHub Copilot (GPT-4.1) on Feb 28, 2026.*
