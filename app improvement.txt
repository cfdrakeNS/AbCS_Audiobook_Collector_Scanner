## app improvement what are your thoughts on this idea.
Vs code can tell when I have a screen reader running both for jaws & nvda. The terminal in vs code will say "Warning: PowerShell detected that you might be using a screen reader..." 
If we can find a way to detect screen readers we could hide the alt+/ message for sighted or low vision user. Thus eliminate confusion what the alt+/ does. 

additional info: Nvda work fine with the app with one exception the alt+/ doesn't work well it will start to speak but is over spoken by nvda itself. However, the read status command in nvda does work. Alt+/ I believe is a timing issue and can be corrected. If we can tell which screen readers is active we could auto adjust the timing or have a setting in preferences for status bar timing.

We tried to detect screen reader in the about window using a windows flag but that didn't work as it always says screen reader active. What approach are you thinking of for detecting jaws or nvda. 

create a plan and name it Access_improvement_mar11  detailing changes required to 
1. when no screen reader is active hide alt+/ shortcut  in f1 menus in all windows.
2. leave alt+/ active as it won't effect anything if pressed 
3. get nvda to read status bar using alt+/, might be timing 
4 add a to preferences a timing setting for read status bar (only visible if screen reader is active)
