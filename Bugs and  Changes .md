Changes for book detail window

* **bd#9 New** button make it alt+s remove alt+v
* **bd#10** if Shortcuts alt+n next or alt+prev previous remove then
* **bd#11** if shortcut f8 or F9 exist in any window remove them
* **bd#12**  the files fields is mings but in on the same line as bitrate before bitrate.Make the same change in import Detail window  
* **bd#13** Review all fields in book detail windows and try to have logical shortcut rearrange if required and update shortcut menu
* **bd#14** add the same status bar message handlers as in main\_window
* **bd#15** When the save button is activated display a message saying name of field that is dirty and Press al-s Save or alt=C Cancel
* **bd#16** Hide the Delete button when save is active and when new is pressed.
* **bd#17** alt keys that are not being used are being entered as test e.g. alt+a enters text in the fields - wuggestions? The fix will have to be done for all windows. 
