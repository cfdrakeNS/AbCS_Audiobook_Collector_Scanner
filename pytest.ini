[pytest]
pythonpath = src test
testpaths = test
python_files = test_*.py
markers =
    slow: tests that take more than a few seconds
    network: tests that intentionally exercise network code (must mock)
    ui: tests that construct Qt windows
addopts = --durations=15
