ABCS COPILOT + JAWS QUICK REF (ULTRA-SHORT)

SESSION START:
Notepad mode, one fix at a time, one file only, minimal patch, include done check.

DEFAULT MODE:
- Short replies
- Plain text first
- One task/file/function at a time
- Small patches only
- No speculative edits
- Accessibility-first (JAWS/NVDA)

USER REQUEST TEMPLATE:
File: src/preferences.
Function: ...
Change: single behavior
Do not touch: ...
Done when: exact expected result

COPILOT RESPONSE TEMPLATE:
1) Goal
2) File
3) Replace this
4) With this
5) Done check

DONE CHECK (EVERY CHANGE):
- git diff -- <file> shows expected edit
- No syntax/runtime error
- Shortcuts still work
- Status text is screen-reader friendly
- Matches "Done when"

ACCESSIBILITY GUARDRAILS:
- Keep scaling/font model intact
- Keep meaningful status bar announcements
- Preserve shortcut conventions
- No raw SQL in UI layer
- Keep changes small/reversible

RUN LOG (JAWS-FRIENDLY):
py src/main.py *> run.log

GOLDEN RULE:
If a change is not visible in file diff, it did not happen.
