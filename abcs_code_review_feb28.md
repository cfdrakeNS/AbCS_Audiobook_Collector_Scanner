# AbCS Code Review – Feb 28, 2026

## Summary
This report lists orphaned code, unused functions/classes, dead code, unreachable code, unused imports, broken references, and other potential issues found in the AbCS codebase as of Feb 28, 2026.

---

## Findings

### 1. Orphaned/Unused Code
- Several cleanup methods exist in query classes (e.g., `cleanup_unused` in `AuthorQueries`, `SeriesQueries`, `GenreQueries`) and are called in [src/ui/main_window.py](src/ui/main_window.py), but check if they are needed in all contexts.
- Some methods in `import_scanner_20260227.py` and `import_scanner.py` appear similar or duplicated. Confirm which file is active and remove obsolete versions.
- There are multiple fallback and correction logic blocks in import scanner modules. Some may be redundant or unreachable due to repeated checks for placeholders and fallbacks.

### 2. Dead/Unreachable Code
- In `import_scanner.py` and `import_scanner_20260227.py`, fallback logic is sometimes repeated after already being applied, which may never be reached.
- Some error normalization and flagging logic is duplicated and could be consolidated.

### 3. Unused Imports
- No major unused imports found in the scanned files, but some `import re` statements are repeated inside functions and could be moved to the top of the file.

### 4. Broken References
- No broken references detected in the scanned files, but ensure that all error/correction flag strings are handled consistently in validator and import window logic.

### 5. TODO/FIXME/Deprecated
- No explicit TODO or FIXME comments found in the scanned files, but some logic is marked as fallback or correction and may need review for completeness.

### 6. Accessibility
- All major UI controls in preferences and import windows have accessible names/descriptions. The fallback controls for author/title were updated from combo boxes to checkboxes for clarity and accessibility. Initialization and signal connection order was corrected to resolve runtime errors. No unrelated logic was changed.

### 7. Miscellaneous
- Multiple versions of import scanner modules exist. Remove or archive unused versions to avoid confusion.
- Some logic in error filter and validator could be simplified for maintainability.

---

## Recommendations
- Remove or archive unused/duplicate modules (e.g., old import scanner files).
- Consolidate repeated fallback/correction logic in import scanner and validator.
- Move repeated imports (e.g., `import re`) to the top of files.
- Review and refactor error/correction flag handling for consistency.
- Confirm that all cleanup methods are necessary and used appropriately.
- Ensure all new UI controls meet accessibility requirements.
- Confirm only one setup_ui method exists per window, and that layout is always initialized at the top of setup_ui. Fallback checkboxes are now properly initialized and connected, with no duplicate layout usage.

---

## Next Steps

## Step-by-Step Cleanup & Refactor Plan (with Testing)

### 1. Remove or Archive Unused/Duplicate Modules
	- Identify obsolete files (e.g., old import scanner versions).
	- Move unused files to an archive folder or delete after review.
	- **Testing:** Confirm main app and import workflow run without errors.

### 2. Consolidate Fallback/Correction Logic
	- Refactor repeated fallback/correction logic in import scanner and validator modules.
	- Ensure only one clear logic path for each fallback/correction scenario.
	- **Testing:** Run import scenarios with various error/fallback cases; verify correct flagging and error handling.

### 3. Move Repeated Imports to Top of Files
	- Relocate repeated imports (e.g., `import re`) to the top of relevant files.
	- **Testing:** Run affected modules to confirm no import errors.

### 4. Refactor Error/Correction Flag Handling
	- Standardize how error/correction flags are set and checked across validator and import window.
	- **Testing:** Scan books with known error/correction cases; verify UI and status bar reflect correct flags.

### 5. Review Cleanup Methods in Query Classes
	- Confirm that `cleanup_unused` methods are necessary and used appropriately.
	- Remove or update as needed.
	- **Testing:** Run database cleanup from main window; verify no orphaned records and no errors.

### 6. Accessibility Review for UI Controls
	- Check all new/modified controls for accessible names/descriptions and keyboard shortcuts.
	- **Testing:** Use screen reader and keyboard navigation to verify accessibility compliance.

### 7. Final Code Review & Documentation
	- Review all changes for maintainability and clarity.
	- Update documentation to reflect refactored logic and file structure.
	- **Testing:** Full regression test of app features and accessibility.

---

*Testing should be performed after each major step to ensure stability and correctness before proceeding to the next.*

---

*Generated by GitHub Copilot (GPT-4.1) on Feb 28, 2026.*
## Completed 
- The scan loop indentation and auto_added fix in import_window.py.
- That the duplicate detection bug is now resolved and tested.
