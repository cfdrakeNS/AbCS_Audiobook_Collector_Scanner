Main window 
1. move the plot button so it is after find button 
2. add shortcut key alt+p that toggles the filter 
3. add a read filter toggle button put it after the plot button 
4. add a shortcut key alt+r
5 update f1 help and shortcuts.py etc. as required 


web metadata window 
1. change status bar msg for plot to say plot found or no plot
2. to improve the screen reader access; when the window opens move the focus to title then to plot this should make plot read.
3. if no plot hide the plot field 
4. check the web series number logic. If a title has a number when you search it seems the web series will have a number.


Book detail window - don't like the layout as the fields move horizontally and vertically depending on the contents of the fields. Seems kind of jumbled up. Propose a cleaner layout that is more visually appealing.  
