# AbCS - Audio Book Collector Scanner
# Requirements for Python 3.9+
#
# Linux system packages (apt — NOT installable via pip):
#   sudo apt install -y libxcb-cursor0 libxkbcommon-x11-0 libgl1 libegl1
#   sudo apt install -y libxcb-xinerama0 libxcb-icccm4 libxcb-image0 \
#     libxcb-keysyms1 libxcb-render-util0 libxcb-shape0
# Required for PySide6 xcb platform plugin. See linux_build.md if the app
# fails with "Could not load the Qt platform plugin xcb".

# GUI Framework
# PySide6 6.7+ Qt builds require SSE4.2/POPCNT on x86_64.
# Keep Python <=3.12 on a compatible Qt line for older CPUs.
PySide6>=6.6.0,<6.7; python_version < "3.13"
PySide6>=6.10.0; python_version >= "3.13"

# Audio metadata reading
mutagen>=1.47.0

# Utilities
python-dateutil>=2.8.2
Send2Trash>=1.8.2
requests>=2.31.0
jinja2>=3.0.0
pandas>=2.0.0
openpyxl>=3.1.0
odfpy>=1.4.1
numpy<2.0

# Image processing
Pillow>=10.0.0

# Screen reader detection (required for Windows JAWS/NVDA support, optional on Linux)
psutil>=5.9.0

# Development/Testing (optional)
pytest>=7.4.0
pytest-qt>=4.2.0
