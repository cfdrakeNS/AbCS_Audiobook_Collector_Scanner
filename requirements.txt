# AbCS - Audio Book Collector Scanner
# Requirements for Python 3.10+ (PySide6 6.11+ minimum; tested on Python 3.14.6)
#
# Linux system packages (apt — NOT installable via pip):
#   sudo apt install -y libxcb-cursor0 libxkbcommon-x11-0 libgl1 libegl1
#   sudo apt install -y libxcb-xinerama0 libxcb-icccm4 libxcb-image0 \
#     libxcb-keysyms1 libxcb-render-util0 libxcb-shape0
# Required for PySide6 xcb platform plugin. See linux_build.md if the app
# fails with "Could not load the Qt platform plugin xcb".

# GUI Framework
# PySide6 6.11+ requires Python 3.10+ and Qt builds require SSE4.2/POPCNT on x86_64.
# 6.11 brings Qt 6.11 accessibility improvements: top-level window/focus fixes (Windows),
# Orientation attribute for sliders, Switch role, and AT-SPI Collection (Linux/Orca).
PySide6>=6.11.1

# Audio metadata reading
mutagen>=1.47.0

# Utilities
python-dateutil>=2.8.2
Send2Trash>=1.8.2
requests>=2.31.0
jinja2>=3.0.0
pandas>=2.0.0
openpyxl>=3.1.0
odfpy>=1.4.1
# numpy 2.x is required for Python 3.14+ (1.x has no 3.14 wheels).
# numpy 2.x is also safe on Python 3.12/3.13 — AbCS uses only pandas
# public API (pd.notna, pd.read_csv, pd.read_excel) which is compatible.
numpy>=1.26

# Image processing
Pillow>=10.0.0

# Screen reader detection (required for Windows JAWS/NVDA support, optional on Linux)
psutil>=5.9.0

# Development/Testing (optional)
pytest>=7.4.0
pytest-qt>=4.2.0

# User doc generation: pip install pypandoc && python scripts/generate_user_docs_odt.py --install-pandoc
pypandoc>=1.13
